spring:
  application:
    name: spring-cloud-eureka
  zipkin:
    base-url: http://192.168.137.101:9411

server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # 服务端需要配置为false
    fetch-registry: false # 服务端需要配置为false
